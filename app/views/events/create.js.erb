console.log('create event and favourite');
document.getElementById("<%= @event.name.gsub(/\s+/, '').gsub(/\W+/, '') %>").innerHTML = '<%= link_to event_favourite_path(Event.find_by(name: @event.name), current_user.favourites.find_by(event_name: @event.name)), method: :delete, id: Event.find_by(name: @event.name).name.gsub(/\s+/, "").gsub(/\W+/, ""), class: "btn-red-index", remote: true do %><i class="fas fa-heart" style="font-size:20px; background: rgba(0,0,0,0);"></i><% end %>';

