document.getElementById("<%= @event.name.gsub(/\s+/, '') %>").innerHTML = '<%= link_to event_favourite_path(@event, @favourite), method: :delete, remote: true do %><i class="fas fa-heart"></i><% end %>';

